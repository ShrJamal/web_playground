{
  "name": "playground",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "jsdom": "^20.0.2",
    "typescript": "^4.8.4",
    "vite": "^3.1.4",
    "vitest": "^0.24.5"
  }
}
